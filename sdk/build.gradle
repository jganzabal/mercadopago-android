apply plugin: 'java'


artifacts {
    archives jar
}

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    compile 'com.google.android:android:2.2.+'
    compile 'com.squareup.retrofit:retrofit:1.5.1'
}
